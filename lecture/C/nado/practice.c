#include <stdio.h>

int main (void)
{
    int i;
    int length;

    printf("How many length ? : ");
    scanf("%d", length);

    for (i=0; i<length; i++)
    {
        printf("a");
    }
    return(0);   
}